---
- name: reload audit rules
  ansible.builtin.command: augenrules --load
  changed_when: true

- name: disable auditd
  ansible.builtin.systemd:
    name: auditd
    state: stopped
    enabled: false
