---
- name: restart sshd
  ansible.builtin.systemd:
    name: "{{ 'sshd' if ansible_facts['os_family'] == 'RedHat' else 'ssh' }}"
    state: restarted

- name: reapply management routing
  ansible.builtin.systemd:
    name: management-network-routing.service
    state: restarted
