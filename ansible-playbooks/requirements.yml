---
collections:
  # community.general.terraform -- plays/vm/provision_vms.yml.
  - name: community.general
    version: ">=11.2.1"
  # ansible.posix.profile_tasks/timer callbacks -- ansible.cfg.
  - name: ansible.posix
    version: ">=2.1.0"
