---
- name: Create a VM from a golden cloud-init template
  hosts: all
  connection: local
  gather_facts: false
  vars:
    ansible_python_interpreter: "{{ ansible_playbook_python }}"

  roles:
    - hypervisor
